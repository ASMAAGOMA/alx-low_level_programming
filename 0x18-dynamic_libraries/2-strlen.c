#include "main.h"
#include <string.h>

/**
 * _strlen - returns the lenght of string
 *@s: indicates to the string
 *Return: the value of string lenght
 */
int _strlen(char *s)
{
	return (strlen(s));
}
