#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strcmp - a function that compares two strings
 * @s1: pointer indicates to the string to be compared
 * @s2: pointer indicates to the string to be compared
 * Return: returns the strcmp value
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
